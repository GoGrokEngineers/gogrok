Pack Funtions to manage Monotonous works

Feruzbek:
    Prior:
        Code(syntax) Formatter(code evalution)✅
        Test-cases(JSONFILED research)✅

        Setup ssh keys of server and github to the ci_cd.yml file
        Configure githubu branches
        Configure migrations if needed?
        Delete all migrations by saving databse file

    Secondary:
        Logoo✅


Musokhon:
    Prior:
        Manage(Create, Delete) files (code evaluation)✅

        Configure Compose file
        Setup Linting Runner inside ci_cd.yml
        Cook statistics

    Secondary:
        Logo✅
