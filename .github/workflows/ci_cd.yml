name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up SSH for Lish access
      - name: Set up SSH for Lish access
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa lish-eu-central.linode.com >> ~/.ssh/known_hosts  # Lish domain

      # Configure Linode CLI (Non-Interactive)
      - name: Configure Linode CLI (Non-Interactive)
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
        run: |
          mkdir -p ~/.config/linode-cli
          echo "[DEFAULT]" > ~/.config/linode-cli/config
          echo "token = $LINODE_TOKEN" >> ~/.config/linode-cli/config

  docker-build:
    runs-on: ubuntu-latest
    needs: setup

    steps:
      # Checkout the code again
      - name: Checkout code
        uses: actions/checkout@v3

      # Build Docker image and run container using Lish SSH
      - name: Build Docker image and run container remotely
        run: |
          ssh -o StrictHostKeyChecking=no feruzbek23@lish-eu-central.linode.com "
            cd /root/gogrok && \
            sudo docker build -t gogrok . && \
            sudo docker run -d --name gogrok gogrok"
        
      # Wait for container to stabilize
      - name: Wait for container
        run: |
          sleep 20  # Adjust time based on your container startup time

      # Optional: Stop and remove the container
      - name: Stop and remove container remotely
        run: |
          ssh -o StrictHostKeyChecking=no feruzbek23@lish-eu-central.linode.com "sudo docker stop gogrok && sudo docker rm gogrok"
